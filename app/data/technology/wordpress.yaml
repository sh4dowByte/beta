id: nginx

info:
  name: Wordpress
  author: sh4dowByte
  severity: info
  description: Wordpress CMS
  metadata:
  tags: cms

match:
  - matchers-condition: or
    matchers:
      - type: regex
        part: body
        regex:
          - 'wordpress'
          - 'WordPress'
        condition: and