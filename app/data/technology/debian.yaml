id: debian

info:
  name: Debian
  author: sh4dowByte
  severity: info
  description: Debian Server
  metadata:
  tags: server

match:
  - matchers-condition: or
    matchers:
      - type: regex
        part: header
        regex:
          - 'Debian'
        condition: and