id: apache

info:
  name: Apache
  author: sh4dowByte
  severity: info
  description: Apache Server
  metadata:
  tags: server

match:
  - matchers-condition: or
    matchers:
      - type: regex
        part: header
        regex:
          - 'Apache'
        condition: and