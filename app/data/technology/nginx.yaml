id: nginx

info:
  name: Nginx
  author: sh4dowByte
  severity: info
  description: Nginx Server
  metadata:
  tags: server

match:
  - matchers-condition: or
    matchers:
      - type: regex
        part: header
        regex:
          - 'Apache'
        condition: and