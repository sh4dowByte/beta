id: ubuntu

info:
  name: Ubuntu
  author: sh4dowByte
  severity: info
  description: Ubuntu Server
  metadata:
  tags: server

match:
  - matchers-condition: or
    matchers:
      - type: regex
        part: header
        regex:
          - 'Ubuntu'
        condition: and